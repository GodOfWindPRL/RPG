/** Phạm vi Slash trên FE (VFX). Phải khớp `SLASH_RANGE` trong `backend/src/modules/combat/world.gateway.ts`. */
export const SLASH_EFFECT_RANGE = 4.2;

/** Base crit settings (UI). Keep in sync with `backend/src/modules/combat/world.gateway.ts`. */
export const PLAYER_BASE_CRIT_RATE = 0.1;
export const PLAYER_BASE_CRIT_MULT = 1.5;
